export default function SelfTests() {
  return (
    <section className="panel p-4 text-sm text-slate-300">
      <h2 className="font-semibold text-slate-50">Ready</h2>
      <p className="mt-1">Upload a parquet file to inspect rows, schema, and charts. Unit tests are available with npm test.</p>
    </section>
  );
}
